package main

// Test struct (value type) vs object (reference type) semantics

type S struct {
    x int
    y int
}

type O object {
    x int
    y int
}

// More complex types with multiple field types
type Person struct {
    age    int
    height int
    score  int
}

type ObjPerson object {
    age    int
    height int
    score  int
}

type Container struct {
    name  string
    items []int
}


func main() {
    // Test 1: Struct value semantics
    println(1000)
    s1 := S{10, 20}
    s2 := s1
    s2.x = 100
    println(s1.x)  // 10
    println(s2.x)  // 100
    
    // Test 2: Object reference semantics
    println(2000)
    o1 := O{10, 20}
    o2 := o1
    o2.x = 100
    println(o1.x)  // 100
    println(o2.x)  // 100
    
    // Test 3: Multi-field struct
    println(3000)
    p := Person{11, 22, 33}
    println(p.age)     // 11
    println(p.height)  // 22
    println(p.score)   // 33
    
    // Test 4: Slice literal
    println(4000)
    items := []int{1, 2, 3}
    println(len(items))  // 3
    
    // Test 5: Struct with string and slice fields
    println(5000)
    c := Container{"hello", items}
    println(len(c.name))   // 5
    println(len(c.items))  // 3
    
    println(9999)
}
