[package]
name = "gox-codegen-llvm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LLVM code generation backend for GoX"

[dependencies]
gox-common = { path = "../gox-common" }

[dev-dependencies]
