[package]
name = "gox-common-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types for GoX (no_std compatible)"

[features]
default = ["std"]
std = []

[dependencies]
num_enum = { version = "0.7", default-features = false }
